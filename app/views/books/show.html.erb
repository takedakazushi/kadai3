<div>    <p id="notice"></p>
        <div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
   <%= image_tag @user.get_profile_image(100,100) %>
  <tbody><tr></tr>
  <tr>
	  <th>name</th>

	  <th></th>
  </tr>
  <tr>
    <div></div>
	  <th>introduction</th>
	  <th></th>
  </tr>
</tbody></table>
  <%= link_to edit_user_path(@book.user.id), class: "btn btn-outline-secondary btn-block" do %>
  <i class="fas fa-user-cog"></i>
  <% end %>
      <h2 class="mt-3">New book</h2>
      <%= form_with model: @book do |f| %>
 <h4>Title</h4>
 <%= f.text_field :title %>
 <h4>Opinion</h4>
 <%= f.text_area :body %>
 <h4></h4>
 <%= f.submit "Create Book" %>
<% end %>
      <!--<form _with model: @books= do |f| %> <input type="hidden" name="authenticity_token" value="" autocomplete="off">-->

<!--</form>-->
  </div>
    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <table class="table">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
      <tr>
        <td> <%= image_tag @book.user.get_profile_image(100,100) %></td>
        <td> <%=link_to @book.title, book_path(@book.id) %></td>
        <td><%= @book.body%></td>
      </tr>
  </tbody>
</table>
</div>

